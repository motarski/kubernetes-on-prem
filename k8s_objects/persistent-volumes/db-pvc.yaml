apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db-data-claim
  namespace: default
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 2Gi
  selector:
    matchLabels:
      storage-type: db-data
